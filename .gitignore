node_modules/
dist/
.env
*.log

# data dumps — never commit these (gigabytes; re-download instead)
data/
*.csv
*.jsonl
*.jsonl.gz
*.zip

# GI source supplements (copyrighted) and scratch output — not redistributed
*.pdf
*.docx
_*.txt
